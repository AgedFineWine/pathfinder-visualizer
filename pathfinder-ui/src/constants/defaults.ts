import { EdgeType, Mode } from "./enums";

export const defaultMode: Mode = Mode.Move;
export const defaultEdgeType: EdgeType = EdgeType.Weighted;
export const defaultNodeType = 'circularNode';
export const radius = 50;
